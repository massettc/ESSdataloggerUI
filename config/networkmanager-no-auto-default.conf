[main]
# Prevent NetworkManager from auto-creating 'Wired Connection N' fallback
# profiles for ethernet interfaces.  The pi-network-admin app manages all
# ethernet connections explicitly through persistent keyfiles in /etc/.
no-auto-default=*
